// Copy this file to secrets.h (same folder as the .ino) and fill in your credentials.
// secrets.h is listed in .gitignore — it will NOT be committed to GitHub.
const char* WIFI_SSID = "YOUR_WIFI_SSID";
const char* WIFI_PASS = "YOUR_WIFI_PASSWORD";
